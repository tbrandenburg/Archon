# Dry-run fixture: code, seams, simplify, and tests run by default while the
# classifier selects docs. The fixture keeps deterministic code stubbed; the
# loader test protects the classifier-binding seam itself.
impl__record-start: '{"start":"recorded"}'
impl__implement:
  done: true
  green: true
  red_cause: ""
  summary: "stub: implemented and green"
impl__assert-changed: "3 files changed"
gate-green: '{"gate":"green","red_cause":"","stage":"The implementation","summary":""}'
pr__pr: { repo: { host: "github.com", path: "example/repo" }, number: 2, url: "https://github.com/example/repo/pull/2", head: "fixture-branch", base: "dev", is_draft: true }
classify:
  errors: false
  docs: true
  reasons:
    errors: "stub: no failure paths"
    docs: "stub: changed shipped docs need impact review"
resolve-scope: '{"errors":"false"}'
review__mode: '{"continuation":false}'
review__scope:
  docs: false
review__code: "0 findings"
review__seams: "0 findings"
review__simplify: "0 findings"
review__tests: "0 findings"
review__docs: "0 findings"
review__review-complete: "settled"
review__synthesize:
  ready: true
  action: none
  findings_summary: "stub: selected review is clean"
  report: { type: archon_artifact, run_id: "fixture-run", path: "review/report.md" }
gate-ready: '{"ready":"true"}'
validate__validate:
  green: true
  red_cause: ""
  summary: "stub: full gate green"
gate-validated: '{"gate":"green","red_cause":"","stage":"The project gate","summary":""}'
ci-probe: '{"state":"concluded","detail":"no checks configured on this repository — nothing to await"}'
ci-verdict: '{"state":"concluded","detail":"no checks configured on this repository — nothing to await"}'
ci-attention-route: '{"attention":false,"red_cause":""}'
flip-ready: { pr_url: "https://github.com/example/repo/pull/2" }
outcome: { pr_url: "https://github.com/example/repo/pull/2", summary: "https://github.com/example/repo/pull/2" }
fixture:
  expect: completed
  reached: [review__code, review__seams, review__simplify, review__tests, review__docs]
