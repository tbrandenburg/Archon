# Dry-run fixture: the ready path. Fully stubbed — proves the DAG wiring and
# that the verdict fields bind. Structured stubs are YAML mappings (they hydrate
# structuredOutput); plain-string stubs stay strings.
plan:
  ready: true
  summary: "stub: approach chosen — full plan at plan.md"
  report: { type: archon_artifact, run_id: "fixture-run", path: "plan.md" }
fixture:
  expect: completed
