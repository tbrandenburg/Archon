# Dry-run fixture: the blocked path. ready:false is a VALID outcome — the run
# must COMPLETE (consumers gate on the field), never fail on honesty.
plan:
  ready: false
  summary: "stub: blocked on missing intent — the plan names the owner's open question"
  report: { type: archon_artifact, run_id: "fixture-run", path: "plan.md" }
fixture:
  expect: completed
