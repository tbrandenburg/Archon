# Sparse guard fixture: run with --default-stubs --exec-code. The assessment is
# explicit, every delivery node stays skipped, and the real outcome node returns
# the no_action reason with the report path.
assess:
  action: no_action
  summary: "stub: the locked version already satisfies the advisory's fixed range"
  report: { type: archon_artifact, run_id: "fixture-run", path: "upkeep-assessment.md" }
fixture:
  expect: completed
exec-code: true
