# Dry-run fixture: the inconclusive path. rooted:false is a VALID outcome —
# the run must COMPLETE (consumers gate on the field), never fail on honesty.
investigate:
  rooted: false
  summary: "stub: two hypotheses survive — the report names what would separate them"
  report: { type: archon_artifact, run_id: "fixture-run", path: "investigation.md" }
fixture:
  expect: completed
